#include "main.h"
/**
 *int _strlen - gives length of the string
 *@s: pointer to string
 * Return: 0 when correct
 */
int _strlen(char *s)
{
	char *s;
	int len;
	s[] = "My first strlen"
	len = _strlen(s);
	return(len);
}
