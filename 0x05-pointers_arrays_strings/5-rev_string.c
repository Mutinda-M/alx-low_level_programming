#include "main.h"
/**
 * rev_string- reverses a string
 * @s: string
 * Return: void
 */
void rev_string(char *s)
{
	int n, m;
	char s;

	n = 0;
	while (s[n]  =! '\0')
	{
		n++;
	}
	for (m = n - 1; m >= 0; m--)
	{
		s[m];
	}
}
