#include <stdio.h>
/**
 * main - prints in both lower case and upper case
 *
 * Return: Returns 0 when correct
 */
int main(void)
{
	for (int x = 'a'; x <= 'z'; x++);
	{
		putchar(x);
	}
	for (int y = 'A'; y <= 'Z'; y++);
	{
		putcher(y);
	}
	putcher('\n');
	return (0);
}	
